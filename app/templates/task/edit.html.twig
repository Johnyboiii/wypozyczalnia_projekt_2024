{% extends 'base.html.twig' %}

{% block title %}Edit Asset{% endblock %}

{% block body %}
    <h1>Edit Asset</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-primary">{{ 'Save'|trans }}</button>
    {{ form_end(form) }}
{% endblock %}